

bool char_to_hex(char input, char & output);

// Combine the nibbles 'ln' and 'rn' to form an output char
char combine_nibbles(char ln, char rn);

void show_message(const QString & Message);
